doctype html
link(rel='stylesheet' href='css/style.css')

head
    meta(charset="UTF-8")
    title My First Full Stack
body
    h1 Simplistic To-do List
    p To add a new to-do task please enter below and click "Add"
    form#todo-form(action="/", method="POST")
        input(type="text", name="todo", placeholder="Add a new todo item", required)
        button(type="submit") Add

    h1 The List of Items 
    p Here is your list of all things that you need to do

    ul
    - for (let i = 0; i < tasks.length; i++)
        li= tasks[i].id + " -> " + tasks[i].task
            form(action="/delete", method="post")
                input(type="hidden", name="id" value=tasks[i].id)
                input(type="submit", value = "Delete")